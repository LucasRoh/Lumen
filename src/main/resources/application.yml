spring:
    datasource:
        url: jdbc:postgresql://localhost:5432/lumen?currentSchema=lumen
        username: postgres
        password: 1234
    jpa:
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                dialect: org.hibernate.dialect.PostgreSQLDialect
                format_sql: true
        show_sql: true